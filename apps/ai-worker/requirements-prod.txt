# Production dependencies for AI Worker
# This file includes all heavy dependencies required for full functionality

# Core dependencies (from requirements.txt)
-r requirements.txt

# PDF Processing
# Requires C++ build tools on Windows
pymupdf==1.24.0

# Document conversion
# Large download ~500MB
docling==2.10.0

# BullMQ Python client
# For consuming jobs from Redis queue
bullmq==0.4.0

# OCR (Optional - Install separately if needed)
# Very large download ~1GB+
# Uncomment the line below only if OCR functionality is required:
# easyocr==1.7.1
