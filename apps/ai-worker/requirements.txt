# apps/ai-worker/requirements.txt
# Core
fastapi==0.126.0
uvicorn==0.38.0
pydantic-settings==2.12.0
httpx==0.28.0
structlog==24.4.0

# Phase 2 ML Dependencies
sentence-transformers>=2.3.0
langchain>=0.3.0
langchain-text-splitters>=0.3.0
torch>=2.2.0 --index-url https://download.pytorch.org/whl/cpu

# ====================
# OPTIONAL DEPENDENCIES
# ====================
# The following are heavy dependencies that require special setup.
# Install them separately if needed for full functionality.

# PDF Processing (requires C++ build tools on Windows)
pymupdf==1.26.0

# Document conversion (large download ~500MB)
docling==2.15.0

# OCR (very large ~1GB+)
# pip install easyocr==1.7.1

# Phase 4 Dependencies
ftfy>=6.1.0
chardet>=5.2.0
pandas>=2.2.0
beautifulsoup4>=4.12.3
lxml>=5.1.0
markdownify>=0.11.6
ebooklib>=0.18
openpyxl>=3.1.2

# Phase 5: Hybrid Embeddings (dense + sparse)
fastembed>=0.4.0
